# Credentials for the "Any Given Sunday" league (private ESPN league, id 544449541).
# Log into ESPN in a browser, open DevTools -> Application -> Cookies -> espn.com,
# and copy these two cookie values. They expire roughly yearly.
ESPN_S2_AGS=
SWID_AGS=
