// Fill this in after deploying the Apps Script Web App described in the
// README's "Settings sheet" section (Extensions > Apps Script > paste
// tools/apps-script/settings_sync.gs > Deploy > Web app > copy the URL here).
// Leave as null to run the Settings page in read-only mode.
export const SETTINGS_WEBAPP_URL = null;
