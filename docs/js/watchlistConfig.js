// Fill these in after (1) adding a "Watchlist" tab (header row: league_slug |
// team_id | player_id) to a Google Sheet shared as "Anyone with the link can
// view", and (2) pasting tools/apps-script/watchlist_sync.gs into that
// sheet's Extensions > Apps Script, deploying it as a Web App (Execute as
// "Me", access "Anyone"), and copying the resulting URL below. See the
// README's "Watch list sheet" section. Leave both null to keep the watch
// list working locally only (per-browser, no cross-device sync).
export const WATCHLIST_SHEET_ID = null;
export const WATCHLIST_WEBAPP_URL = null;
